import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.enterprise.dualcrypt.auth",
  appName: "DualCrypt Authenticator",
  webDir: "dist",
};

export default config;
